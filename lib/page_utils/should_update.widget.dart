import 'package:flutter/material.dart';

class ShouldUpdate extends StatelessWidget {
  const ShouldUpdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
